export {
  PERMISSIONS,
  ROLE_PERMISSIONS,
  ROLES,
  getPermissionsByRole,
  getPermissionsByRoles,
  hasPermission,
  hasRole,
} from '@/lib/authz/permissions';

export type {
  Permission,
  Role,
  UserPermissions,
} from '@/lib/authz/permissions';
